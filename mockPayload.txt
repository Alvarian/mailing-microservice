const outBoundNotifications = {
    errors: [],
    warnings: []
}

if (outBoundNotifications.errors.length || outBoundNotifications.warnings.length) {

    fetch("service.com/api/logger", {
        method: "POST",
        body: JSON.stringify(outBoundNotifications)
    })

}
